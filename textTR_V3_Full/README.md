# Text File Splitter & Merger with Translation

โปรแกรมสำหรับแบ่ง รวม และแปลไฟล์ข้อความ พร้อมฟีเจอร์ขั้นสูง

## 📁 โครงสร้างโปรเจค

```
textTR-main/
├── main.py                 # Entry point หลัก
├── run_gui.bat             # Batch file สำหรับรันโปรแกรม
├── requirements.txt        # Dependencies
├── README.md
│
├── config/                 # การตั้งค่าและค่าคงที่
│   ├── __init__.py
│   └── constants.py
│
├── core/                   # Logic หลักของโปรแกรม
│   ├── __init__.py
│   ├── text_splitter.py    # แบ่ง/รวมไฟล์
│   └── translation_engine.py # Engine แปลภาษา
│
├── data/                   # จัดการข้อมูล
│   ├── __init__.py
│   └── translation_data.py # ข้อมูลการแปล
│
├── gui/                    # GUI components
│   ├── __init__.py
│   ├── base.py             # Base classes
│   ├── main_window.py      # Main window
│   └── tabs/               # แต่ละ tab
│       ├── splitter_tab.py
│       ├── merger_tab.py
│       ├── translation_tab.py
│       ├── viewer_tab.py
│       └── settings_tab.py
│
├── utils/                  # ฟังก์ชันยูทิลิตี้
│   ├── __init__.py
│   ├── file_utils.py       # จัดการไฟล์
│   ├── json_utils.py       # จัดการ JSON
│   └── ui_utils.py         # UI utilities
│
├── samples/                # ไฟล์ตัวอย่าง
│   └── json_files/
│       ├── sample_list.json
│       ├── sample_dict.json
│       └── sample_game_items.json
│
└── (ไฟล์เดิมยังคงใช้งานได้)
    ├── main_gui.py
    ├── gui_base.py
    ├── file_operations.py
    ├── file_viewer.py
    ├── translation_manager.py
    ├── text_splitter.py
    ├── ai_translator.py
    ├── utils.py
    └── constants.py
```

## ฟีเจอร์หลัก

### 🔄 การแบ่งและรวมไฟล์
- แบ่งไฟล์ข้อความขนาดใหญ่เป็นไฟล์เล็กๆ
- รวมไฟล์ที่แบ่งแล้วกลับเข้าด้วยกัน
- รองรับการแบ่งตามจำนวนบรรทัด

### 👁️ การดูไฟล์
- ดูไฟล์ข้อความแบบเรียลไทม์
- นำทางแบบทีละบรรทัด
- รีเฟรชอัตโนมัติ

### 🌐 การแปลข้อความ (ฟีเจอร์ใหม่!)

#### การจัดการการแปล
- **แปลทีละบรรทัด** หรือ **แปลทั้งไฟล์**
- **ปิด/เปิดการแปลในแต่ละบรรทัด** - สามารถกำหนดให้ข้ามบรรทัดใดบรรทัดหนึ่งไม่ให้แปล
- **📍 แปลเฉพาะส่วนที่ต้องการ** - แปลเฉพาะข้อความหลังเครื่องหมายแบ่งเท่านั้น (รองรับ `:` `=` `|` `->` `=>` `~` `#` `@` และเครื่องหมายอื่นๆ)
- แสดงผลแบบตาราง พร้อมตัวอย่างข้อความ
- รองรับหลายเครื่องมือแปล

#### วิธีใช้การปิด/เปิดการแปล
1. **คลิกที่คอลัมน์ "ข้าม"** ในตาราง เพื่อสลับสถานะ
2. **ใช้ Checkbox** ในส่วนแก้ไข เมื่อเลือกบรรทัดแล้ว
3. **ปุ่มจัดการทั้งหน้า:**
   - "ข้ามหน้านี้ทั้งหมด" - ปิดการแปลทุกบรรทัดในหน้า
   - "เปิดหน้านี้ทั้งหมด" - เปิดการแปลทุกบรรทัดในหน้า
   - "สลับสถานะหน้านี้" - สลับสถานะทุกบรรทัด

#### สัญลักษณ์ในตาราง
- 🚫 = บรรทัดที่ข้ามการแปล (ไม่แปลเมื่อแปลทั้งไฟล์)
- ✅ = แปลแล้ว
- ⏳ = รอการแปล
- ✓ = เปิดใช้การข้าม

#### เครื่องมือแปลที่รองรับ
- **Google Gemini AI** (แนะนำ) - รองรับ Custom Prompts และ Text Protection
- Google Translate API
- deep-translator
- googletrans
- พจนานุกรมพื้นฐาน

### ⚙️ การตั้งค่า
- จัดการไฟล์และโฟลเดอร์
- ดูสถิติการใช้งาน
- ทำความสะอาดไฟล์เก่า

## การใช้งานเบื้องต้น

### 1. แปลข้อความ
1. เปิดแท็บ "แปลข้อความ"
2. เลือกไฟล์ที่ต้องการแปล
3. ตั้งค่าภาษาต้นฉบับและเป้าหมาย
4. **กำหนดบรรทัดที่ไม่ต้องการแปล** (ถ้ามี):
   - คลิกที่คอลัมน์ "ข้าม" ในตาราง
   - หรือเลือกบรรทัดแล้วติ๊ก checkbox "ข้ามบรรทัดนี้"
5. แปลทีละบรรทัด หรือแปลทั้งไฟล์

### การใช้ Gemini AI
1. ใส่ API Key ของ Google Gemini
2. เลือกประเภทการแปล (นิยาย, เกม, สนทนา, ฯลฯ)
3. ตั้งค่า Text Protection เพื่อป้องกันข้อความพิเศษ เช่น {ตัวแปร}, [แท็ก]

### 3. การแปลเฉพาะส่วนที่ต้องการ (Separator Translation)
สำหรับไฟล์ที่มีรูปแบบ `ID+เครื่องหมายแบ่ง+Content` เช่นไฟล์เกมหรือ config files:
1. เปิดใช้ "แปลเฉพาะข้อความหลังเครื่องหมายแบ่งเท่านั้น"
2. เลือกเครื่องหมายแบ่ง: `:` `=` `|` `->` `=>` `~` `#` `@` หรือกำหนดเอง
3. ทดสอบกับข้อความตัวอย่าง เช่น `Cook_1: Fried Rice` หรือ `ItemID|Magic Sword`
4. แปลปกติ - ระบบจะแปลเฉพาะส่วนหลังเครื่องหมายแบ่ง

### 4. ไฟล์ตัวอย่าง
- `test_all_separators.txt` - ตัวอย่างไฟล์ทุกเครื่องหมายแบ่งที่รองรับ
- `test_separator_feature.py` - สคริปต์ทดสอบฟีเจอร์เครื่องหมายแบ่ง
- ไฟล์อื่นๆ สำหรับทดลองฟีเจอร์การแปล

## ข้อกำหนด

### Python Modules
```bash
pip install tkinter googletrans deep-translator requests
# สำหรับ Gemini AI (ไม่บังคับ)
pip install google-generativeai
```

### ไฟล์ที่จำเป็น
- `main_gui.py` - แอปพลิเคชันหลัก
- `translation_manager.py` - จัดการการแปล
- `ai_translator.py` - Gemini AI (ไม่บังคับ)
- `constants.py` - ค่าคงที่
- `utils.py` - ฟังก์ชันช่วย
- และไฟล์อื่นๆ

## การรันโปรแกรม

### Windows
```bash
python main_gui.py
# หรือ
run_gui.bat
```

### Linux/Mac
```bash
python3 main_gui.py
```

## ตัวอย่างการใช้งาน

### การแปลโดยข้ามบางบรรทัด
```
1. Hello world          ✓ แปล → สวัสดีชาวโลก
2. Your HP is {HP}      🚫 ข้าม (เก็บตัวแปรไว้)
3. Press [ENTER]        🚫 ข้าม (เก็บคำสั่งไว้)  
4. Welcome!             ✓ แปล → ยินดีต้อนรับ!
```

### การแปลเฉพาะหลังเครื่องหมายแบ่ง
```
# เครื่องหมาย : (โคลอน)
1. Cook_1: Fried Rice     → Cook_1: ข้าวผัด
2. NPC_Guard: Hello!      → NPC_Guard: สวัสดี!

# เครื่องหมาย = (เท่ากับ)  
3. Name=John Smith        → Name=จอห์น สมิธ
4. Language=English       → Language=ภาษาอังกฤษ

# เครื่องหมาย | (ไพพ์)
5. Item|Magic Sword       → Item|ดาบวิเศษ
6. Type|Adventure Game    → Type|เกมผจญภัย

# เครื่องหมาย -> (ลูกศร)
7. Quest->Find treasure   → Quest->ค้นหาสมบัติ
8. Action->Attack enemy   → Action->โจมตีศัตรู
```

เมื่อแปลทั้งไฟล์ โปรแกรมจะแปลเฉพาะส่วนที่ต้องการตามการตั้งค่า

## เคล็ดลับการใช้งาน

1. **สำหรับเกม**: ข้ามบรรทัดที่มี {ตัวแปร}, [แท็ก], หรือคำสั่งพิเศษ
2. **สำหรับโค้ด**: ข้ามบรรทัดที่เป็นคอมเมนต์หรือคำสั่ง
3. **สำหรับไฟล์ใหญ่**: แปลเป็นชุดๆ และใช้การแบ่งหน้า
4. **บันทึกงาน**: บันทึกการแปลเป็นระยะเพื่อป้องกันข้อมูลสูญหาย

## การแก้ปัญหา

### ปัญหาการแปล
- ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
- ลองเปลี่ยนเครื่องมือแปล
- ตรวจสอบ API Key (สำหรับ Gemini)

### ปัญหาการแสดงผล
- รีเฟรชตาราง (กด F5)
- ปิดเปิดโปรแกรมใหม่

## License

Open Source - ใช้งานและปรับปรุงได้อย่างอิสระ